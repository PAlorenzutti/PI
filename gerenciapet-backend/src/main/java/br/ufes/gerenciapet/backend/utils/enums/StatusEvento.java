package br.ufes.gerenciapet.backend.utils.enums;

/**
 * Estados de ciclo de vida de um evento.
 */
public enum StatusEvento {
    /** Evento aberto para inscrições ou participação. */
    ABERTO,
    /** Evento em execução. */
    EM_ANDAMENTO,
    /** Evento finalizado. */
    ENCERRADO
}
